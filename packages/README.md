Temporary directory with packages needed by the inspector part of the MMIF Server.

These include:

- A new version of mmif-python (sporting a version number that
it in the end may not have) that includes the summarizer and that may take a while to be available on PyPI.

- An initial version of the MMIF Inspector, which is also not available yet.

Once the above packages are available on PyPI then this entire directory should be deleted.